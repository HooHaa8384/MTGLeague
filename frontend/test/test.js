require('../src/test.js');

describe("A suite", () => {
  it("contains spec with an expectation", () => {
    expect(true).toBe(true);
  });

  /*it("returns true", () => {
    expect(isTrue()).toBe(true);
  });*/
});
